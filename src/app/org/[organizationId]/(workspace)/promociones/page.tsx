import { PromotionsPanel } from '@/src/domains/promotions/components/PromotionsPanel'

/** Ruta del panel de pautas del publisher (dentro del workspace B2B). */
export default function PromocionesPage() {
  return <PromotionsPanel />
}
